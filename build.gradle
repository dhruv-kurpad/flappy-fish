import org.xml.sax.InputSource

import javax.xml.parsers.SAXParserFactory

plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.5'
    id 'io.spring.dependency-management' version '1.1.7'
    id 'org.asciidoctor.jvm.convert' version '4.0.5'
    id 'checkstyle'
    id 'jacoco'
}

checkstyle {
    toolVersion = '11.1.0'
    configFile = file("checkstyle.xml")
    ignoreFailures = false
    maxWarnings = 0
}

bootJar {
    mainClass = 'com.cs506.tutorial.TutorialApplication'
}

group = 'com.cs506'
version = '0.0.1-SNAPSHOT'
description = 'Demo project for Spring Boot'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

ext {
    set('snippetsDir', file("build/generated-snippets"))
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.boot:spring-boot-starter-data-rest'
    developmentOnly 'org.springframework.boot:spring-boot-devtools'
    testImplementation 'com.h2database:h2'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testImplementation 'org.springframework.restdocs:spring-restdocs-mockmvc'
    implementation 'com.mysql:mysql-connector-j'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
    outputs.dir snippetsDir
    useJUnitPlatform()
    finalizedBy jacocoTestReport
}

<<<<<<< HEAD
tasks.named('asciidoctor') {
    inputs.dir snippetsDir
    dependsOn test
}

tasks.register('printJacocoCoverage') {
    dependsOn 'jacocoTestReport'
    doLast {
        def reportFile = file("${buildDir}/reports/jacoco/test/jacocoTestReport.xml")
        if (!reportFile.exists()) {
            println "JaCoCo report not found: ${reportFile}"
            return
        }

        // Configure SAX parser to ignore external DTD
        def factory = SAXParserFactory.newInstance()
        factory.setFeature("http://apache.org/xml/features/disallow-doctype-decl", false)
        factory.setFeature("http://apache.org/xml/features/nonvalidating/load-external-dtd", false)
        def parser = factory.newSAXParser().getXMLReader()

        // Ignore DTD
        parser.setEntityResolver { publicId, systemId -> new InputSource(new StringReader("")) as org.xml.sax.InputSource }

        def xml = new XmlSlurper(parser).parse(new InputSource(reportFile.newReader()))
        def counter = xml.counter.find { it.@type == 'LINE' }
        def covered = counter.@covered.toDouble()
        def missed = counter.@missed.toDouble()
        def total = covered + missed
        def percentage = (int) (total > 0 ? (covered / total * 100) : 0)

        println "COVERAGE: ${percentage}%"
    }
}

jacocoTestReport {
    dependsOn test
    reports {
        xml.required = true
        html.required = true
        csv.required = false
    }
    finalizedBy printJacocoCoverage
}

=======
>>>>>>> kcb/pipeline_setup
